{
    "type": "module",
    "scripts": {
        "build": "rollup --config rollup.config.js && tsc --project tsconfig.d.json",
        "dev": "rollup --config rollup.config.js --watch",
        "declare": "tsc --project tsconfig.d.json"
    },
    "devDependencies": {
        "@rollup/plugin-terser": "^0.4.4",
        "@rollup/plugin-typescript": "^11.1.6",
        "rollup": "^4.18.1",
        "tslib": "^2.6.3"
    },
    "volta": {
        "node": "20.10.0"
    },
    "engines": {
        "node": ">=20.10.0"
    }
}
